{# {% extends '_templates/base.njk' %}

{% set relative_path = "../" %}

{% block page_title %}{{ slug }}{% endblock %}

{% block page_description %}{{ slug }}{% endblock %}

{% block content %}
  <article class="container">
    <div class="row ">
      <div class="col-sm-5">
        <img src="../img/img-mb/{{ image }}" alt="" class="img-fluid mb-2">
      </div>
      <div class="col-sm-7">
        <h1> {{Headline}} </h1>
        {{ prose(post) }}
      </div>
    </div>
  </article>
{% endblock %} #}
